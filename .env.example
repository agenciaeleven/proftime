# Frontend (Railway / local)
# Local com proxy Vite: use /api (sem host)
VITE_API_URL=/api

# Produção Railway:
# VITE_API_URL=https://proftime-api-production.up.railway.app
# Chave administrativa (mesmo valor de ADMIN_SECRET na API)
VITE_ADMIN_SECRET=
